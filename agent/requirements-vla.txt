# Install ONLY in the separate VLA worker environment, using scripts.install_vla.
# CUDA-enabled torch/torchvision must already match the host's JetPack/driver.
# The installer pins their installed versions so pip cannot replace them.
lerobot[smolvla]==0.4.4
transformers==4.57.1
